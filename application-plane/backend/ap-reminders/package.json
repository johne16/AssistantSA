{
  "name": "ap-reminders",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AssistantSA application-plane Reminders service: stores resident reminders (utility bill due dates, civic events, assistant-set reminders), serves them to the client Feed, and fires due reminders through the notifier. Tenant-scoped through the injected store. Holds no credentials.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "~5.8.3"
  }
}
