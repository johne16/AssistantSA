# ap-voice

Rust library crate (`ap_voice`) for the voice path. Run from this folder.

```
cargo check    # type-check without producing a binary
cargo build    # compile (debug)
cargo build --release
cargo test
```

No external crates are declared yet; `cargo` resolves them from `Cargo.toml` when added.
