{
  "name": "ap-notifications",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AssistantSA application-plane Notifications service: registers and stores resident device push tokens and per-type notification opt-ins, receives notify requests from source modules, and delivers pushes to opted-in residents. Generates no content and evaluates nothing; only filters by opt-in and delivers.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "~5.8.3"
  }
}
